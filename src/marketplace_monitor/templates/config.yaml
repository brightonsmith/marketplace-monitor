# Generated by marketmon. Relative paths are resolved from this file's directory.

browser:
  profile_dir: browser-profile
  headless: true
  page_load_timeout_seconds: 45
  scroll_count: 2

database_path: data/marketplace.db
check_interval_minutes: 10
status_interval_minutes: 60

# quiet_hours:
#   start: "22:00"
#   end: "07:00"

notify_on_first_run: false
notify_on_startup: true

notifications:
  provider: console
  ntfy:
    server: https://ntfy.sh
    topic: replace-with-a-long-random-topic

# Add search YAML files with: marketmon add search.yaml -c config.yaml
searches: []
